<h1>All Tags</h1>

<ul id="tags">	
	<% @tag.each do |tags| %>
		<li>
			<%= link_to tag.name, tags_path(tag) %>
		</li>
	<% end %>
</ul>

<%= link_to "delete", tags_path(@tag), method: :delete data: { confirm: "Really delete this tag?" } %>
